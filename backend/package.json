{
  "name": "hubble-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env-shell \"json-server db.json --host 0.0.0.0 --port ${PORT:-3000}\""
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "json-server": "^1.0.0-beta.3"
  }
}
